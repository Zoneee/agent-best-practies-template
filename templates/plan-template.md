# 执行计划

> 先让读者看懂目标与理由，再展开交付质量控制和实现路径。默认使用能覆盖当前任务的最轻结构；不要为了显得完整而把所有章节写满。
> `plan-light` 适用于单任务、单 slice、低风险规划，可把每个章节压缩为 1-3 条直接信息；`full-governance` 适用于跨文件、多阶段、Autopilot 或高风险执行，需要完整阶段、证据和回滚设计。
>
> 文件命名：在 `docs/exec-plans/active/` 中创建新计划时，使用 `YYYY-MM-DD-<topic>.md`，例如 `2026-05-07-template-governance-upgrade.md`。
> 技术债记录继续保留在 `docs/exec-plans/tech-debt-tracker.md` 中，条目使用 `TD-001` 这类独立编号，不使用日期前缀命名。

## 标题

## 状态
草稿 | 执行中 | 被阻塞 | 已完成

## 适用层级
plan-light | full-governance

## 阶段完成度概览
- `plan-light`：可只写当前切片、当前状态和下一步；若没有阶段划分，可明确写“按单切片推进”。
- Phase 1：
- Phase 2：
- Phase 3：
- Phase 4：

## 交付目标与成功标准
- 要解决什么：
- 为什么现在做：
- 为什么选当前路径，而不是更简单或更重的替代方案：

## 范围

## 非范围

## 质量门禁
- 写最小必要门禁即可；`plan-light` 不需要为了形式把所有治理条目铺开，但必须说明当前最关键的验证和边界控制。

## 验证计划
- 先写最能证伪当前方案的验证，再补充其他检查。

## 证据记录方式
- 若需长周期观察、迁移验证或兼容性试点，先使用 `templates/observation-record-template.md` 记录原始 observation card；本计划只摘要稳定结论与关键证据入口。
- observation card 默认放在 `docs/exec-plans/active/observations/` 或与当前计划关联的独立观察目录中，并在本计划的 `进展日志` 中引用。
- 若执行将跨多轮对话、启用 Autopilot 或需要显式的 stop condition，可通过 `.github/prompts/handoff.prompt.md` 或另建 `YYYY-MM-DD_handoff_{topic}.md` 交接契约；该契约只负责执行投影，不替代本计划作为完整真相源。
- `plan-light` 可把证据入口收敛到本计划、PR 或一次 focused validation；只有确实需要跨轮追踪时再扩展为 observation card 或独立 handoff。

## 文档更新与治理
- 受影响文档：
- 是否需要同步修订或移除过时内容：
- 本计划的验证证据将记录在：

## 风险

> `plan-light` 下可只保留 1-3 个主要风险；不要罗列与当前切片无关的背景性风险。

## 回滚与止损

> 写当前切片真的会用到的回滚方式；若没有可回滚产物，可明确写“以停止继续扩张改动面作为止损”。

## 背景

## 问题摘要

## 受影响区域

## 约束与禁止

## 实现步骤

> 每一步都要细到可以直接执行或交接，不接受只有 Phase 标题或抽象动词的步骤。
> 默认每步至少写明：本步目标、输入依赖、涉及文件 / 模块、具体动作、完成证据、进入下一步前的检查。
>
> 推荐写法：
> 1. 本步目标：
>    - 输入依赖：
>    - 涉及文件 / 模块：
>    - 具体动作：
>    - 完成证据：
>    - 进入下一步前检查：

> 保留这里作为原始计划基线，不要在推进过程中删除已完成步骤或重写本节。
> 推进状态统一更新到 `## 状态`、`## 阶段完成度概览`、`## 进展日志`、`## 决策日志` 和 `## 后续事项`。
> 若后续创建 `plan -> execute handoff contract`，也只是在执行阶段压缩本计划，不得把 handoff contract 反过来当作完整计划替代品。

## 决策日志

## 进展日志

> 只追加新的事实记录，例如步骤完成、当前进度、验证结果。不要重复描述完整状态链。
> 若存在长周期观察记录，不要把逐条 observation card 直接贴到这里；只回写已稳定的结论与决定。

## 后续事项

> 只保留当前剩余动作；不要把已经完成的状态链复制到这里。
