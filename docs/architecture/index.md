# 架构索引

状态：有效
负责人：工程团队
最后评审：（填写日期）

## 概览
本仓库采用显式领域边界和受约束的依赖方向。

## 继续阅读
- `layering.md`：分层规则
- `domain-map.md`：领域映射
- `dependency-rules.md`：依赖规则
- `data-flow.md`：数据流
- `invariants.md`：系统不变量

## 核心规则
- 依赖只能沿允许的层级方向流动
- 边界输入必须经过校验
- 共享关注点通过显式接口引入
- 架构违规必须可被机械检测
